# MFanControl scripts

当前阶段为工程骨架工具说明，后续逐步补齐签名、公证、安装脚本。

- `TODO`: dev/build scripts will be added after core modules are implemented.

推荐执行流程（骨架阶段）：
1. `swift package describe`
2. `swift build`
3. `swift test`
