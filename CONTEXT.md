# MFanControl Context

## 项目边界

本项目定位为：面向 **Apple Silicon Mac** 的热管理系统，而不是通用风扇遥控工具。

- 平台边界：只支持 M1 及以上机型；非 Apple Silicon 为“不支持”状态并进入只读提示。
- 控制边界：核心目标是“在可解释前提下自动平衡安静、温度、性能”，并默认优先保障系统安全。

## 核心词汇

- **系统默认控制（Apple Default）**  
  macOS 仍在管理风扇时的原生控制状态。任何异常时都必须能回退到该状态。

- **App 自动控制**  
  本项目的菜单栏控制循环根据策略下发风扇目标，并对外保留明确的可解释说明。

- **安全回退**  
  下列条件触发时，立即停止本地控制并回退到系统默认控制：  
  - 关键控制链路故障（helper 异常、权限缺失等）  
  - 关键传感器失效到达容错阈值  
  - 温度异常与硬件保护相关告警  
  - 程序崩溃或生命周期保护失败

- **只读模式**  
  硬件控制不可用时，仍可展示传感器与风扇状态，但不允许下发 RPM/风扇目标。

- **风扇能力识别**  
  启动后识别：芯片、机型、风扇数量、转速范围、默认控制状态、可用传感器清单与可靠性。

- **真实传感器读数**  
  来自系统或硬件可验证来源的温度、功耗或风扇数据；不可用时必须显示“不可用”，不得用模拟值替代。

- **最高热点温度**  
  当前可用温度通道中的最高温度摘要，必须能追溯到具体传感器通道。

- **风扇能力异常机型**  
  检测到无风扇机型（如 Air）时，显示“不可控”并固定为只读或系统默认控制，不提供手动/自动控制入口。

- **模式**  
  - `静音`：优先降低噪音与 RPM，允许更高温度下限。  
  - `均衡`：默认模式；更积极于温控且尽量不明显提噪。  
  - `性能`：偏重稳定高负载性能，接受更高风扇噪音。  
  - `自定义曲线`：用户可编辑控制曲线，适用于高级场景。  
  - `App 触发`：检测到白名单应用运行后切入更激进模式。

- **应用触发模式**  
  由白名单进程触发的临时模式。默认白名单可编辑，新增/移除需用户确认。

- **控制来源**  
  对外展示当前控制来源（Apple 默认 / App 自动 / 用户手动 / 安全回退）。

- **热压力分数（thermal_score）**  
  统一 0~100 的热风险指标，来源于温度、功耗、负载持续性与可得传感器综合。用于模式可解释展示与策略入口。

- **降频监测**  
  用于判断“是否因为热而限制性能”的状态感知。支持度低时以“不可用”提示，不阻断风扇控制流程。

- **历史与告警**  
  本地环形日志与聚合统计；告警仅用于关键状态变更（默认/安全回退/异常），高频温度提醒不对用户打扰。

## 关键优先级（语义）

本项目的决策优先级固定为：
1. 安全回退  
2. 用户手动恢复默认  
3. 系统状态触发（如唤醒、供电状态变化）  
4. 应用触发模式  
5. 用户选择模式（静音/均衡/性能/自定义）

## 控制状态机

`SmartControl` 的生命周期约束为：

- `Idle`：只读监控或待授权状态，不下发硬件控制。  
- `Discovering`：正在识别机型、传感器、风扇能力与默认控制状态。  
- `SmartControl`：完成能力识别且满足能力可控、helper 健康、安全条件时才可进入，执行热管理闭环。  
- `SafetyFallback`：异常条件触发后立即回退到 Apple 默认控制。  
- `ManualDefault`：用户点击一键恢复默认后进入，直到用户重新启用控制。  

`SmartControl` 的入口条件：  
- helper 已安装且健康  
- helper 以有效签名身份安装，并且带有可访问 `AppleSMCClient` 的 IOKit temporary exception，能通过 privileged helper 路径写入风扇键  
- 当前机型可控（非无风扇/非 Apple Silicon）  
- 安全条件满足（无关键传感器失效、无链路异常）

从 `SafetyFallback` 或 `ManualDefault` 恢复时，必须先进入 `Discovering` 并完成快速健康探测，再切回 `SmartControl`。

## 控制节奏

- 传感器/系统指标采样：2 秒  
- 控制决策频率：2 秒  
- 控制下发频率：最多每 5 秒一次  
- 单次下发斜坡限制：每步最大 300 RPM  
- 告警去抖：15 分钟  

## 应用触发检测

- 默认白名单为当前确认集合（如 Xcode/Android Studio/JetBrains IDEA/Final Cut Pro/DaVinci Resolve/Ollama/LM Studio/Docker/Blender）。  
- 第一期仅采用进程名匹配（轻量级、低权限）。  
- 默认仅触发临时升级，不采集签名/App 深检；后续版本可按风险与精度再补充更强验证。

## 热压力分数（thermal_score）

- 区间定义：  
  - `0 ~ 30`：安静区  
  - `31 ~ 60`：预警升速区  
  - `61 ~ 80`：加速区  
  - `81 ~ 100`：安全区  
- 持续性约束：温度/功耗等上升趋势在目标提升前需满足持续观测窗口（默认 12~20 秒）以抑制尖峰响应。  
- 默认分量权重：CPU、GPU、SoC、SSD、功耗可见且可被高级用户配置；  
  - 普通用户默认只展示归一化后的 `thermal_score`；  
  - 高级模式下可调整权重，但设置范围受限并保留审计记录。  
- 采样/权重更新建议以“平滑方式”作用于热压力分数，避免单次重配权重引发突变。  

## 安全触发条件

- 单次温度尖峰不应直接触发异常降温措施。  
- 进入 `Safety`/`SafetyFallback` 的温度路径需同时满足：  
  - 关键风险维度连续异常且超过阈值  
  - 连续持续时间超过配置窗口（默认 12~20 秒）  
- 链路异常（helper 失效、传感器关键通道丢失、权限/通讯失败）走立即 `SafetyFallback`。  

## 日志与事件

最小审计事件先记录：  
- `timestamp`  
- `level`（`info` / `warn` / `error`）  
- `state`（`Idle` / `Discovering` / `SmartControl` / `SafetyFallback` / `ManualDefault`）  
- `reason`（如 `safe-fallback` / `manual-default` / `system-event` / `app-boost` / `hardware-write-result`）  
- `payload`（如热压力分数、目标 RPM、当前模式、传感器可用性、控制来源）

## 配置导入导出

- 导出格式：JSON。  
- 必要字段：`schema_version`、`profile`、`weights`、`history_retention`、`whiteList`、`safety_thresholds`。  
- 导入时进行版本兼容与字段范围校验，不通过即拒绝并提示迁移建议。  
- 默认不导出敏感字段（如用户凭据、机型指纹、系统路径）。  

## 数据持久化

- 历史采样与事件日志统一写入本地 SQLite。  
- 建议存放路径：`~/Library/Application Support/MFanControl/`。  
- 历史保留策略：  
  - 1 分钟采样原始数据保留最近 24 小时；  
  - 1 小时聚合数据保留最近 30 天。  

## 应用触发事件

- 应用触发属于状态事件：进入触发时将施加 `performance_boost`，并以默认 20 分钟时间窗计时。  
- 事件可按 10 分钟步长滚动延长（仅当触发条件持续满足）。  
- 触发条件消失时立即恢复到用户选择的基础模式。  
- App 触发事件共用安全回退与告警去抖规则。  
- 基础模式与 App 触发采取叠加策略：  
  - 基础模式提供基线曲线（静音/均衡/性能/自定义）。  
  - App 触发作为临时偏置（加权/上移），而非完全覆盖。  
  - 最终目标仍受安全上限与安全回退规则约束。  

## 卸载与恢复默认

- 卸载流程默认包含：  
  - 提示并先执行一次“恢复 Apple 默认控制”；  
  - 移除 helper 前完成最终恢复动作；  
  - 清理启动项与 XPC 注册；  
  - 清理本地配置与缓存（可保留导出快照）。  
- 用户退出/重启前应进行一次最终状态回退校验。  

## 交付方向

- 首发面向非 App Store 的可签名、可公证、可审计发布链路。  
- 菜单栏为主要交互入口，需支持基础视图与高级视图分层。  
- helper 为受限特权路径，CLI 为控制入口代理，不直接暴露底层硬件直写。  
- 交付按阶段推进：  
  - MVP：机型识别、温度/RPM 监控、智能闭环控制、静音/均衡/性能、崩溃安全回退、睡眠唤醒恢复、一键恢复默认、菜单栏常驻。  
  - 优秀版本：自定义曲线、功耗感知增强、App 触发增强、历史图、降频监测、导入导出、CLI 深化。  
  - 高级功能（后置）：远程监控、ML 学习、时间段策略、合盖专用策略。  
- 实现栈与部署（第一版）：  
  - 主应用与 CLI 使用 Swift（优先 SwiftUI + XPC）。  
  - 特权 helper 由 LaunchDaemon 管理并承担传感器采集与风扇控制底层操作。  
  - 主应用与 helper 通信仅通过受限 XPC 接口。  
